services:
  onionperf:
    image: onionperf
    restart: always
    environment:
      - NOMAD_IP_http_port=localhost
    volumes:
      - ./onionperf-data/:/home/onionperf/onionperf-data/

# docker build . -t onionperf --build-arg="ANON_ENV=dev"
